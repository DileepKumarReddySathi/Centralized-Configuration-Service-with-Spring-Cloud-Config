spring:
  application:
    name: inventory-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
management:
  endpoints:
    web:
      exposure:
        include: "health,refresh"
  endpoint:
    health:
      show-details: always
